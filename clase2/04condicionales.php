<?php

if (true) {
    var_dump('Esto se hace');
}

if (false) {
    var_dump('Esto no se imprime');
}

$polo = 'CSsS';

if ($polo != 'CSS') {
    var_dump('El polo es de CSS');
}else {
    var_dump('Puto esto no sirve');
}

/**
 * < menor que
 * > mayor que
 * <= menor o igual que
 * >= mayor o igual que
 * == igual que
 * != no es igual a
 * === identico a 
 * !== no es identico a
 * <> es diferente de
 */

 